# Stale — v1 stack only

These files serve the **Flask v1** dashboard (localhost:5000). They are NOT
used by the production **Django v2** site (pushinweight.ai).

If you're working on the **Pushin' Weight web dashboard**, the canonical
copies live at:

    monitor/static/

Do not edit these v1 files for v2 changes. They are identical today but
will diverge and eventually be deleted alongside the rest of `x_monitor/`
when v1 is decommissioned.
