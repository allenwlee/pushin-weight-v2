interface:
  display_name: "Ollija"
  short_description: "Annotate one shared PushinWeight delivery plan"
  default_prompt: "Use $ollija to run ./bin/ollija annotate-plan before selecting or creating a plan, enrich the returned plan_path, and refresh its delivery guide after planning or review."
