"""Per-probe package for the classify_batch_pragmatics_full limits probe.

Each probe under scripts/probes/<name>/ owns its own folder per plan 2026-07-15-001
(KTD10). This __init__.py is empty by design — it exists only so
`python -m scripts.probes.<name>.probe` resolves as a module path.
"""