---
title: feat/mockup-v23 plan
artifact_contract: ce-unified-plan/v1
artifact_readiness: implementation-ready
product_contract_source: ollija-annotate-plan
execution: code
ollija:
  change_id: feat-mockup-v23-2026-08-19-043225
  branch: feat/mockup-v23
  workflow: codex-v24-production-ui
  delivery_target: production
  delivery_selected_by_user: true
---
<!-- BEGIN OLLIJA DELIVERY GUIDE -->
## Ollija Delivery Guide

This block is generated guidance. Do not edit it directly. Correct durable facts in `.ollija/project.yaml` or this template, then rerun `./bin/ollija annotate-plan`. Put a user-directed exception in the editable Delivery Exceptions section below.

### Resolved locations

- Authoritative host: `fuchitalee`
- Authoritative repository: `/Users/fuchitalee/development/pushin-weight-v2`
- Ollija release worktree area: `/Users/fuchitalee/development/pushin-weight-v2/.worktrees`
- Active worktree: `/Users/fuchitalee/development/pushin-weight-v2/.worktrees/feat/mockup-v23`
- Plan: `/Users/fuchitalee/development/pushin-weight-v2/.worktrees/feat/mockup-v23/docs/plans/2026-08-19-043225-feat-mockup-v23-plan.md`
- Change: `feat-mockup-v23-2026-08-19-043225`
- Branch: `feat/mockup-v23`
- Staging branch and blueprint: `staging`, `/Users/fuchitalee/development/pushin-weight-v2/.worktrees/feat/mockup-v23/render-staging.yaml`
- Production branch and blueprint: `main`, `/Users/fuchitalee/development/pushin-weight-v2/.worktrees/feat/mockup-v23/render.yaml`
- Staging URL: `https://pushinweight-staging-web.onrender.com`
- Production URL: `https://pushinweight-web.onrender.com`

### Placement

This worktree is inside the Ollija release worktree area. Reuse it for the whole change. Do not create a second worktree or plan for this branch.

### Delivery scope

- Workflow: `codex-v24-production-ui`
- Delivery target: `production`
- Owner selection recorded: `true`

1. Complete implementation and the plan's verification contract.
2. Run the configured focused checks:
   - `pytest tests/ollija`
3. The parent workflow commits only this plan's changes, pushes the feature branch, and records the candidate SHA.
4. Fetch the remote staging lane: `git fetch origin refs/heads/staging`.
5. Require the unchanged candidate SHA to be a fast-forward of that fetched remote ref, then push the exact candidate SHA to `refs/heads/staging` with the server-enforced fast-forward command `git push origin <candidate-sha>:refs/heads/staging`.
6. Verify the remote staging ref resolves to the candidate SHA and the Render deployment for `pushinweight-staging-web` reports that same SHA.
7. Run staging checks. Stop here if they fail.
8. Only after staging passes, fetch the remote production lane: `git fetch origin refs/heads/main`.
9. Require the same unchanged candidate SHA to be a fast-forward of that fetched remote ref, then push the exact candidate SHA to `refs/heads/main` with the server-enforced fast-forward command `git push origin <candidate-sha>:refs/heads/main`.
10. Verify the remote production ref resolves to the candidate SHA and the Render deployment for `pushinweight-web` reports that same SHA before reporting completion.
11. After step 10 succeeds, perform worktree cleanup as the final filesystem action:
    - From `/Users/fuchitalee/development/pushin-weight-v2`, require `/Users/fuchitalee/development/pushin-weight-v2/.worktrees/feat/mockup-v23` to remain registered, clean, unlocked, and at the verified candidate SHA. If any guard fails, retain it and report the reason.
    - Run `git -C /Users/fuchitalee/development/pushin-weight-v2 worktree remove /Users/fuchitalee/development/pushin-weight-v2/.worktrees/feat/mockup-v23` without `--force`.
    - Preserve the local and remote feature branches. Continue final reporting from the authoritative repository root.

### Failure handling

- Never promote a staging candidate whose automated checks failed.
- Implementation failures return to the parent implementation workflow for diagnosis, correction, recommit, and restaging.
- SSH, shell, environment, or multi-machine failures use the repository infra/multi-machine skill first.
- The change ledger is advisory; do not validate or enforce it.
- Never force-remove a worktree. Retain staging-only, failed, dirty, locked,
  noncanonical, or candidate-mismatched worktrees for diagnosis or later
  delivery.
- Do not run an endless retry loop or start a persistent Ollija process.
<!-- END OLLIJA DELIVERY GUIDE -->

## Delivery Exceptions

- 2026-08-19 owner instruction: commit this candidate and deliver it to the
  staging lane for review. Stop after staging verification; do not promote it
  to production as part of this action.
- 2026-08-28 owner instruction: run the newly requested UI work through the
  full LFG production pipeline. This supersedes the 2026-08-19 staging stop
  for the two new release units below; each unit must still pass staging before
  its unchanged candidate SHA is promoted to production.
- The generated cleanup step applies only after Release B is verified in
  production. Retain this canonical clean worktree after Release A production
  verification so Release B can pin that exact baseline and continue here.

# Goal

Apply only the owner-approved V22-to-V24 control changes to the public Django
home page. Keep the production Chart.js chart, feed, endpoints, data contracts,
headline, filters, and all other chrome unchanged. Correct the subsequent
staging-review failures in pulse localization, compressed window-control
layout, and the 30-day chart request without changing those product contracts.

## Product Contract

The approved visual/interaction target is
`docs/ideation/mockups/v24.html`, constrained by
`docs/reference/2026-08-19-132714-v24-bridgewright-target.md`. The written
contract wins wherever the static mockup could otherwise be mistaken for a
production implementation target.

### Approved targets

| Surface | Required behavior |
| --- | --- |
| Locale buttons | `en`, `zh-cn`/`中文`, and `original`/`原文` retain identical button geometry while labels change. |
| Time-window buttons | All four buttons retain identical geometry across English, Chinese, and original modes. The one-day label is `1d` in English/original and `1天` in Chinese. |
| Pulse chips | Each selected chip uses the same blue accent as the active time-window control. Chips toggle independently; selected brands are the union sent to both production chart and feed requests; deselecting the last chip restores all brands. |
| Timezone pill | Local and California times are always visible in 24-hour format, separated by one thin divider. Local is selected initially. Clicking toggles the selected blue half without changing the pill or total topbar-control dimensions. Existing feed timestamp conversion continues to follow the selected timezone. |
| Pulse heading | English/original show `Trending · 1d heat`; Chinese shows `脉冲 · 1天热度`. |
| Compressed windows | All four time-window buttons, including `365d`/`365天`, remain visible and non-overlapping at mobile and compressed-desktop widths. |
| 30-day request | The ordinary 30-day chart request completes without exhausting the web worker timeout; its chart, pulse, and Top Voices payload contracts remain unchanged. |

### Explicit non-targets

- Do not replace or simplify the production Chart.js graph, its datasets,
  scales, legend, interaction, payloads, or `/chart.html` endpoint.
- Do not replace or simplify the production feed, its content, layout,
  ordering, pagination, metadata, or `/feed/` endpoint.
- Do not copy mockup-only polylines, sample brands, hidden-element rules, or
  demonstration attributes into production.
- Do not change the headline, filter taxonomy/panels, internal dashboard,
  authentication, locale middleware, stored data, harvester, or deployment
  topology. The sole schema exception is a non-unique, concurrent covering
  index supporting the existing date-bounded home queries.

## Implementation Units

1. Pin the current behavior through the authenticated/anonymous real Django
   route and add a failing Playwright regression for V24 geometry, copy,
   multi-select request state, timezone behavior, and rich chart/feed
   preservation.
2. Make the smallest template and CSS changes required for fixed control
   geometry, selected pulse styling, and the split timezone pill.
3. Extend the existing shared filter store from sole pulse selection to a
   toggled brand set, and update the chart's pulse re-render projection to
   preserve every selected chip. Leave chart/feed request and rendering code
   otherwise intact.
4. Update timezone rendering so both clocks remain visible while the existing
   active-zone feed timestamp path remains unchanged.
5. Run focused JavaScript, Django, and Playwright tests at desktop and mobile
   widths. Re-run Bridgewright validation/status against the approved V24
   contract and inspect the real rendered page for console errors and layout
   overflow.
6. Replace the inverted pulse gettext entries with English source keys and a
   Chinese `1天` translation, then pin both rendered locale outcomes.
7. Let the four equal-width window segments shrink before the fixed timezone
   pill and assert non-overlap at 320px, 393px, 520px, and desktop widths.
8. Remove the redundant posts subquery from the common chart aggregate and add
   a reversible PostgreSQL covering index with `CREATE INDEX CONCURRENTLY` for
   the three date-bounded home projections.

## Regression Net

- Real route: `/` through Django's locale and authentication middleware, not a
  standalone template fixture.
- Viewports: desktop, 520px compressed desktop, and 393px/320px mobile.
- Locales: English, `zh_hans`/Chinese chrome, and original where control copy
  differs.
- Data flow: one pulse action produces one `/chart.html` request and one
  `/feed/` request with identical multi-brand filters.
- Preservation: the page retains one live Chart.js canvas with production
  datasets and no mockup SVG graph; feed rows and pagination remain rendered
  from the production endpoint.
- Timezone: both clock values match `HH:MM`, selected half toggles, overall
  geometry remains stable, and feed timestamps change zones.
- Quality gate: zero unexpected test skips, browser page errors, console
  errors, failed local assets, or Bridgewright contract/configuration errors.
- Query path: the ordinary aggregate has one bounded post join and no nested
  post-ID subquery; the index migration is non-atomic/concurrent and model
  state remains migration-clean.

## Definition of Done

- All approved target cases above pass in the production code path.
- Every explicit non-target remains intact.
- Focused and relevant existing regression suites pass.
- Commit and push the verified candidate to the staging branch for owner
  review. Do not promote it to production without a later explicit request.

## Verification Record

- Focused red/green browser proof: the three V24 behavior tests initially
  failed on old copy, sole pulse selection, old selected styling, and the
  single-clock timezone markup, then passed after implementation.
- Relevant suite: 52 passed, 32 PostgreSQL-required tests executed, zero
  PostgreSQL skips/errors, and 14 browser subtests passed.
- JavaScript chart regression: 47 passed, zero failed; all four modified
  JavaScript files also pass `node --check`.
- Django: `manage.py check` reports no issues. `git diff --check` and critical
  Ruff syntax/name checks pass.
- Real browser: desktop and 320px Chrome retained one live Chart.js canvas and
  zero mockup SVG charts; DeepSeek plus MiniMax reached both production
  endpoints as one union; split timezone dimensions remained stable and feed
  timestamps switched between local and CA.
- Bridgewright: validation is clean with no findings. Attempt
  `20260819T044659Z-27134c884ea9` is partial only at the external owner/device
  proof boundary; its build identity is clean with no mismatches.
- Staging-review remediation (local): locale/catalog pin passes; all 11
  chart/pulse PostgreSQL tests pass; the three V24 browser flows pass with 14
  subtests across 320px, 393px, 520px, and desktop; Django system check,
  migration drift check, and `git diff --check` pass. One unrelated pre-existing
  catalog assertion about removed untranslated keys remains red and is not
  changed by this plan.

## Production filter/feed batch (2026-08-19)

### Target boundary

The protected baseline is production commit
`d821c4b7188c7df8100efec7d21195d9e1277d58`. The approved delta is defined by
`docs/reference/2026-08-19-174833-production-filter-feed-bridgewright-target.md`:
post-type filtering and ordered pills, zh-CN dropdown labels, an uncategorized
discourse bucket, the existing feed text cycle, scoped feed-row X links, and
independent English/Original locale selection. The substantial Chart.js batch
is explicitly deferred; the existing chart is protected except for applying
the new filter predicates.

### Implementation units

1. Pin the production baseline and new Bridgewright target/non-target contract.
2. Add failing real-route, queryset, JavaScript, and browser regressions for
   each approved interaction and the protected Chart.js/feed paths.
3. Add the post-type pill and localized filter-entry projection without
   changing stable filter machine keys.
4. Add synthetic uncategorized discourse semantics to the shared feed/chart
   predicates without adding a stored taxonomy row or schema change.
5. Reuse the V24 text-layer cycle in production feed hydration and add a
   guarded row-level X link that excludes text, handles, links, and signals.
6. Correct locale active-state comparison so English and Original are mutually
   exclusive.
7. Verify focused tests, Django checks, JavaScript syntax, a real desktop and
   mobile browser, and the local Bridgewright contract harness. Do not commit,
   stage, or deploy without a later owner instruction.

### Regression net

- The six named pills retain their exact relative order; preserved Discourse
  and Unsanctioned follow them.
- All zh-CN visible dropdown labels are Chinese while request keys remain
  unchanged.
- Uncategorized discourse agrees between in-memory feed filtering and the
  existing set-based chart queryset.
- Text cycling and row navigation are tested as separate click targets.
- English, zh-CN, and Original each produce exactly one active locale button.
- The production Chart.js canvas, payload shape, endpoints, feed shape,
  pagination, and V24 controls remain intact.

### Verification record

- Focused PostgreSQL/Django net: 92 passed, 35 required PostgreSQL tests
  executed, zero skips/errors, plus 40 subtests.
- Full real-browser module: 26 passed with 20 browser subtests after the
  manual-QA server and pytest were isolated from one another.
- JavaScript: chart contract 47/0 and feed contract 46/0; modified scripts
  pass `node --check`.
- Django: `manage.py check` reports no issues and `makemigrations --check`
  reports no model drift. `git diff --check` passes.
- Agent-browser desktop and 393px mobile inspection: required eight-pill
  order, fully localized zh-CN dropdown choices, visible uncategorized
  discourse, synthesis/literal-Chinese/English wrap, one live Chart.js canvas,
  no mockup SVG chart, no horizontal page overflow, and no browser console or
  page errors.
- Bridgewright: the local contract/configuration harness passes and records
  production baseline `d821c4b7188c7df8100efec7d21195d9e1277d58` with the
  substantial Chart.js batch explicitly excluded. No Bridgewright executable
  is installed in this checkout, so no external Bridgewright run is claimed.

### Commentary-field addendum

The owner confirmed the previously intended `posts.commentary_zh_cn` column
was never created and additionally approved a nullable `commentary_en` column.
This addendum narrows the implementation to schema, persistence, and feed-wire
correction:

1. Add nullable `commentary_zh_cn` and `commentary_en` text columns in migration
   `0016`, after the branch's concurrent-index migration `0015`.
2. Persist the translator's already-produced `cn_equivalent` value only to
   `commentary_zh_cn`, treating blank and `N/A` as `NULL`.
3. Leave `commentary_en` unpopulated. Add no prompt, model, endpoint, batch,
   call, token spend, or retry behavior.
4. Carry both fields through the production feed wire. Chinese text cycling
   uses available commentary, literal Chinese, then English; missing and
   duplicate layers are skipped.
5. Do not backfill history, migrate a live database, deploy, or modify the
   protected Chart.js component in this batch.

Regression net: pin both model columns, exercise the real Django
`CycleRunner` translator-to-Post path, assert `commentary_en` remains `NULL`,
assert the SSR and refreshed JSON feed carry both fields, and exercise the
three-layer interaction in a real browser. The concurrent backfiller branch is
left untouched and must rebase across this branch's `0016` migration before
merge.

Verification: the production schema/cycle/UI slice passes 13 PostgreSQL-
required tests with zero skips or errors; the full affected browser modules
pass 33 tests plus 20 viewport/locale subtests; the feed JavaScript contract
passes 49 assertions. Migration drift, Django system checks, critical Ruff,
JavaScript syntax, diff hygiene, and the local Bridgewright target harness are
clean. `commentary_en` is asserted `NULL`, and the production-call-chain net
asserts exactly one existing translator call and one classifier call.

## Owner-directed usability continuation (2026-08-28)

### Delivery organization

Use two ordered production release units inside this canonical plan and
worktree. Release A groups the six routine feed/headline changes because they
share the same row wire shape, templates, SVG sprite, CSS, and refreshed-DOM
path. Release B follows only after Release A is verified in production because
hover-freeze introduces transient Chart.js state, a bounded-time feed query,
and cross-component cancellation behavior. Do not combine the two candidates,
and do not begin Release B delivery while Release A remains unverified.

The protected baseline for Release A is production SHA
`e1447569c4097f2abc6af110358f10afc33a5168`. Release B must pin the exact
verified production SHA produced by Release A as its own baseline. The
Cyber-Quan runtime sprite and the production typography, color system,
controls, chart, feed ordering, authentication, `/internal/`, persistence,
harvester, and deployment topology remain protected unless an explicit
requirement below changes them.

### Shared success contract

- Every requested interaction works on the authenticated public `/` route in
  English and zh-CN at desktop, 390px, and 320px widths, including both the
  server-rendered first batch and rows/headlines replaced by JavaScript.
- Every release's Bridgewright affected and candidate assessments have zero
  failed, skipped, errored, missing, or unknown obligations. Exact screenshots
  and geometry assertions protect non-target regions and the 320px no-overflow
  boundary.
- The deployed candidate preserves all Cyber-Quan icons already in production:
  every SVG `<use>` resolves to nonzero geometry, semantic colors remain
  correct, and blue remains reserved for selected states.
- Relevant JavaScript, Django, real-browser, locale, static-provenance, and
  project regression tests pass with no new console or page errors.

## Release A — feed and headline usability polish

### Product contract

1. **Bounded text expansion.** A feed text click still advances one text layer,
   and the containing row grows naturally for the selected layer. The default
   three-line mobile/four-line desktop clamp becomes at most nine/twelve lines
   while expanded, so the total row never exceeds three times its default
   height. Short text is fully visible; long text remains present in the DOM
   but visually clamped. Clicking outside text restores the default clamp.
2. **zh-CN layer order.** zh-CN cycles `综合` (synthesis), `直译` (literal),
   then `原文` (the source text), never the English translation. Missing or
   byte-identical layers are skipped without producing blank steps. English
   and Original modes retain their current layer contracts.
3. **Headline disclosure.** Every schema-3 headline secondary is hidden by
   default. A localized `detail`/`详情` link-style button appears at the end of
   the title and reveals only that item's secondary. A localized `hide`/`收起`
   link follows the secondary; activating it, clicking the secondary itself,
   or pressing Enter/Space on the secondary hides it. `hidden`,
   `aria-expanded`, `aria-controls`, focus behavior, and unique IDs stay in
   sync. A chart/headline refresh recreates the collapsed default.
4. **Filter option symbols.** Options prepend the same Cyber-Quan symbol used
   for that meaning in feed signals. Sentiment preserves green/slate/red/amber;
   post types use their exact six feed symbols; discourse, nationalism, and
   unsanctioned use their existing generic feed symbols. Brand and language
   options gain no invented symbol. SVGs are decorative and labels remain the
   accessible names. Dropdown geometry and the single capture-phase open-state
   controller remain unchanged.
5. **Account-role signal.** Under every follower count, reserve a compact role
   slot. `official`, `staff`, and `community` use one new rough Cyber-Quan
   credential-badge outline (`icon-role-badge`) colored amber `#fbbf24`, violet
   `#c4b5fd`, and green `#86efac`, respectively; `other` and absent roles show
   no icon. A deterministic precedence of official, staff, then community
   selects the one displayed role when an account has multiple brand roles,
   without changing role-filter matching. The localized role label supplies
   `title` and accessible text, and both initial and refreshed rows use the
   same projection without per-row queries. Move the current follower
   `role="img"` semantics onto a follower-only child so the independent role
   badge is not nested inside another image role; an empty reserved role slot
   is hidden from assistive technology.
6. **One-line identity header.** The account display-name link truncates with a
   real ellipsis before the immutable age/timestamp/status metadata can wrap.
   The handle and all adjacent metadata together remain one line at 320px and
   larger, with the link's full value still available through its existing
   destination and a title/accessible name.

### Implementation units

1. Add a Release-A Bridgewright target rooted at the exact baseline SHA. Model
   text collapsed/expanded/layer states, headline collapsed/expanded states,
   filter dropdown open/interior/exterior states, three role states plus empty,
   initial/refreshed rows, locales, and responsive widths before product edits.
2. Write failing JavaScript and real-browser regressions for all six contracts.
   Browser tests must assert visible geometry rather than class presence alone:
   row-height bounds, line counts, on-screen dropdown rectangles, persistent
   interior checkbox interaction, exterior close, headline `hidden` state,
   one-line header boxes, resolved SVG geometry, and no page overflow.
3. Extend the existing Cyber-Quan sprite with only `icon-role-badge` and keep
   it `currentColor`-driven. Centralize the production key-to-symbol and
   key-to-tone projection so the filter entries and feed renderer cannot drift.
4. Update the feed serializers and SSR/client renderers to expose and render
   the deterministic localized display role, the full selected text layer,
   bounded expansion, and the one-line identity header. Preserve feed row
   navigation exclusions, pagination, auto-refresh, filters, and signal order.
5. Add one delegated headline disclosure controller that works for SSR and
   atomically replaced schema-3 items. Do not alter narrative generation,
   storage, selection, freshness, legacy-schema rendering, or chart refresh
   transactions.
6. Project icon metadata into localized dropdown entries and render decorative
   SVGs without changing stable filter keys, checkbox defaults, filter-store
   semantics, portal placement, capture-phase ownership, or dirty-dot logic.
7. Run the focused tests, Bridgewright affected/candidate gates, deterministic
   EN/zh-CN screenshots, and real browser regression net. Deliver Release A as
   one exact SHA through staging and production, then verify production before
   starting Release B.

### Release-A regression net

- Text: first click both cycles and expands; subsequent clicks keep the row
  bounded; outside click collapses; zh-CN is synthesis/literal/source; keyboard
  activation matches pointer; a >600-character refreshed row is not data-
  truncated; row navigation never opens X from a text-layer click.
- Headline: each item opens independently, hide link and secondary click close,
  keyboard/ARIA state is correct, refresh resets collapsed, and schema 1/2 plus
  empty/failure states remain unchanged.
- Filters/icons: all mapped options resolve to nonzero symbols in EN and zh-CN;
  sentiment and role tones are exact; dropdown remains on-screen, survives an
  interior checkbox click, closes outside, and keeps current request keys.
- Roles: server and refreshed rows match for official/staff/community/empty and
  multi-role precedence; role filtering still evaluates the complete role set.
- Identity: long CJK, Latin, and unbroken handles ellipsize while metadata stays
  visible on one line at 320/390/desktop; no horizontal overflow or signal-
  column displacement appears.
- Protected behavior: Chart.js payload/datasets/tooltip, locale persistence,
  timezone conversion, filter requests, infinite scroll, refresh races, row
  links, Cyber-Quan masthead/follower/signal icons, and `/internal/` are intact.

## Release B — 24-hour Chart.js hover-freeze

### Protected baseline

Release A is verified in production at immutable release SHA
`e2d48a2c642ccbf03407a7a1ecfd36161ab0f018` (product-source SHA
`7b9ede538fbd1f50ad7db165bb9d6e48d0b2537d`). At `2026-08-28T08:27:08Z`,
the production web deployment `dep-da8kba1t0dsc73c73hdg`, headline-worker
deployment `dep-da8kba1t0dsc73c73h6g`, and harvest deployment
`dep-da8kba9t0dsc73c73hng` were all `live` at that exact release SHA. The
production ref, login health, `/internal/` protection, static bundle hashes,
desktop interactions, zh-CN three-layer order, and 320px no-overflow boundary
were independently verified. This exact SHA is Release B's protected baseline.

### Product contract

1. Hover tooltips in the 24-hour chart show a prettified bucket datetime in
   both browser-local time and Beijing time, followed by the existing per-brand
   post counts. The display uses `Intl.DateTimeFormat`, remains correct across
   date rollover and DST, and does not alter stored locale/timezone preference.
2. Clicking a real 24-hour chart point freezes that bucket: Chart.js keeps the
   point and tooltip visibly active, all locale buttons become visually
   unselected without changing the underlying locale, and the feed title is
   replaced by the same localized local/Beijing datetime.
3. While frozen, the feed shows every post whose `created_at` is within that
   exact five-minute half-open bucket. Only the current brand selection still
   filters those posts; sentiment, post type, role, language, nationalism,
   discourse, and unsanctioned settings are ignored for the frozen feed but
   remain stored and unchanged.
4. A subsequent click anywhere other than the frozen point exits freeze,
   clears the persistent tooltip/point, restores the previously active locale
   button and localized `Latest in window`/`本窗口最新` title, and atomically
   refetches the feed under the complete current filter state. The click that
   releases freeze must not activate a stale bucket.
5. Freeze is transient: it is never written to localStorage, cookies, URLs, or
   server session. Chart/feed periodic refresh cannot dislodge the active
   tooltip or overwrite the frozen feed; normal refresh resumes on exit.
   Seven-, 30-, and 365-day charts do not enter hover-freeze.

### Implementation units

1. Pin Release A's verified production SHA and add a separate Release-B
   Bridgewright target/state model for hover, frozen, release, locale visual
   suppression/restoration, exact feed request, periodic refresh, and stale-
   response races.
2. Add a small ephemeral hover-freeze controller at the Chart.js/feed boundary.
   Derive `[start,end)` from the payload's selected five-minute label, retain
   Chart.js active elements/tooltip with animation disabled, snapshot only the
   locale presentation state, and dispatch explicit freeze/start/end events.
3. Extend `/feed/` with validated `freeze_start` and `freeze_end` ISO timestamps.
   Accept them only together for the one-day path, require an aware positive
   interval no longer than five minutes inside the active 24-hour horizon, and
   apply those bounds in the database query before the hard cap. During this
   mode normalize the filter projection to brands only. Malformed or out-of-
   range requests fail closed with HTTP 400; no schema change is allowed.
4. Teach the feed request gate, pagination, end sentinel, title, and refresh
   timer about the transient range. Abort superseded freeze/unfreeze requests
   so a late response can never replace the newer state. Pause chart and feed
   periodic refresh while frozen and resume immediately on release.
5. Add local/Beijing Chart.js title callbacks and reuse the formatter for the
   feed heading. Keep existing series label/count callbacks, dataset order,
   legend, filters, colors, scales, and timezone row-label plugin unchanged.
6. Run backend interval validation/query tests, deterministic JavaScript tests,
   real-canvas Playwright clicks at known scale coordinates, Bridgewright
   affected/candidate gates, and EN/zh-CN desktop/mobile visual regressions.
   Deliver Release B as a second exact SHA through staging and production.

### Release-B regression net

- Point hit-testing freezes the intended index and five-minute interval; empty
  chart space, non-point clicks, and non-24-hour charts do not start freeze.
- Tooltip and feed heading show correct local and Beijing dates/times across
  midnight and DST; the existing per-brand counts remain visible and ordered.
- Frozen `/feed/` results include boundary-start posts, exclude boundary-end
  posts, apply selected brands, ignore every other filter, and paginate only
  inside the frozen range without exceeding the hard cap.
- Locale buttons have zero selected states only during freeze; actual locale,
  text layers, localStorage, timezone, lenses, window, and filter values never
  change. Exit restores the exact prior locale selection and title copy.
- Periodic chart/feed timers and out-of-order network responses cannot erase a
  freeze or resurrect it after exit. One release click produces one normal feed
  replacement, and all timers resume without duplication.
- Existing chart refresh atomicity, headline/pulse/top-voices projection,
  dataset visibility, filter pills, text/headline disclosure, role icons,
  pagination, Cyber-Quan geometry/colors, 320px layout, and `/internal/`
  protection remain green.

### Release-B local implementation record

- Product-source SHA:
  `7b799ea51f44aec269b5c5b9f2fec5f98dbd9939`.
- Approved target:
  `docs/reference/2026-08-28-181416-chart-hover-freeze-bridgewright-target.md`.
- The affected Bridgewright gate closes all 2,804 obligations with zero failed,
  skipped, errored, missing, or unknown results. Its runtime net passes 32
  Python tests plus 10 subtests, 99 chart assertions, 80 feed assertions, and
  the existing timezone checks.
- Real Chromium proves exact rendered-point selection and persistent tooltip
  state at 390px English and 320px zh-CN, including brand-only five-minute feed
  requests, locale suppression, no horizontal overflow, and full release-state
  restoration. The 320px run caught and fixed overlapping point-hit ambiguity
  by requiring nearest-point geometry.
- PostgreSQL proves inclusive-start/exclusive-end range behavior and eight
  malformed/out-of-horizon failure cases. Django system checks, migration-drift
  checks, JavaScript syntax, `git diff --check`, and 74 Ollija tests are clean.
