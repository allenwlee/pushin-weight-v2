# Qwen3-235B-A22B-Instruct-2507 translation v2 trial

`qwen235_translation_v2` changed only the frozen v1 profile's `max_concurrency` from its prior setting to `1`. The model, GMICloud FP8 route, prompt, structured-output wire shape, sampler, catalog price caps, no-retry rule, and no-fallback rule were unchanged. This isolates serial delivery as the experiment factor.

The diagnostic24 contract is `.context/model-task-20260917/qwen235-translation-v2-diagnostic24-20260917-124322/contract.json`. Its 55 generated requests produced 20 received responses and 35 HTTP 429 transport failures, leaving 18 of 24 sources without every required locale. No retry, fallback, or database write occurred. Provider receipts totalled $0.0015662325 for 8,879 input and 2,787 output tokens. Request-latency p50 was 1.131 seconds, p95 was 9.946 seconds, and the maximum was 18.589 seconds.

The serial ordering does not support a precise quota claim. Requests 0000--0013 returned 429, 0014--0020 were received, 0021--0026 returned 429, 0027--0037 were received, 0038--0052 returned 429, and 0053--0054 were received. The retained records contain no HTTP response headers or error bodies, so they cannot establish an exact RPM limit or prove a fixed rate window. They do establish that serial dispatch alone did not make this route reliable.

The source-only packet is `.context/model-task-20260917/qwen235-translation-v2-diagnostic24-20260917-124322/source-only-review-packet.json` (SHA-256 `b1029cc7a21334bd69bbb99998ff9d68081a54d1e79532e46b15cd2287c3d05c`). The complete 72-locale status record is `source-grounded-review-v2.json` (SHA-256 `2c2bfd0019526a1204f10a36303a929d537eb60414bde665e58bb5c1b5a122d4`). It has 35 locale coverage errors spanning 18 sources, 16 exact native copies, and 21 delivered generated outputs left explicitly unreviewed because the transport-invalid run is already ineligible. Its source-error interval is [18,24], versus the incumbent [7,8]; conservative R113 parity is not established.

Do not spend a v3 call on a prompt-only revision while GMICloud FP8 delivery remains this unreliable. If the next edit window approves a bounded rate diagnosis, use the same profile and prompt with `max_concurrency=1` plus `min_request_interval_seconds=4`, measured from start to start and recorded in the contract. That is a non-retry control for possible provider-window throttling, not evidence of a known 15 RPM quota. At roughly 55 generated requests per 24-source diagnostic, it adds at least 216 seconds of scheduled spacing; the retained random-100 translation set has 215 generated targets (85 native copies), so its scheduled spacing alone is about 856 seconds before completion and long-request gaps. That leaves a tight, but not mathematically impossible, 900-second operating gate. The other captured routes exceed the saved $0.0875/M input and $0.35/M output caps, so switching to them would violate the trial's price constraint.
