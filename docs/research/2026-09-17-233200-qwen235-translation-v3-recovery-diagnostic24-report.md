# Qwen235 v3 recovery diagnostic24

Exception 35 authorized one recovery-aware rerun of the frozen Qwen v3 diagnostic contract. The rerun retained the frozen Qwen model, GMICloud FP8 route, prompt, JSON response shape, price caps, and four-second request-start interval. Only HTTP 429/503 recovery changed.

The contract at `.context/model-task-20260917/qwen235-translation-v3-diagnostic24-20260917-125905/contract.json` has SHA-256 `232b3f70cd72cbcc249f35ceec6f44b001d35e8e95355e8a10157b9568c006ad`. The isolated rerun is at `.context/model-task-20260917/qwen235-translation-v3-recovery-rerun-diagnostic24-20260917-230735/`; it verified the full contract and request-list digests before sending.

The run made 66 attempts for 55 frozen requests between `2026-09-17T14:09:46.822784Z` and `2026-09-17T14:31:17.038455Z`. It returned 52 translations, recovered 4 requests whose first receipt was a 429, and exhausted three requests after three 429s each. It charged `$0.0044253475` from received usage receipts. The isolated worst-case reservation was `$0.1556223375`; it left the recorded Qwen cumulative reservation at `$0.4815640375` and portfolio reservation at `$8.0852708375`.

All retained 429 bodies identify `limit_source=upstream_provider_shared_pool`, `provider_name=GMICloud`, and `provider_error_code=rate_limit_exceeded`, with `Retry-After: 60`. The service later recovered on the same pinned route, so this is evidence of a transient upstream shared-pool limitation, not evidence that a replacement provider, prompt, or model configuration was tried. The first stopped preflight process remains separately retained with four HTTP 200 responses, `$0.0002031225` in receipts, that failed only because its missing `X-OpenRouter-Metadata` header prevented strict provider attestation; it is runner diagnostics, not translation-quality evidence.

The runner recorded every attempt’s exact send/end timestamps, whitelisted retry/rate headers, request ID when returned, usage receipt, first/final outcome, and sanitized error body. It used at most three attempts per target, retried only 429/503, used Retry-After otherwise exponential jittered backoff, shared the cooldown across sequential requests, and preserved four-second starts. Its fake evidence covers a strict validated 429-to-200 recovery, 503 backoff, non-retry 400, three-429 cap, deadline skip, and redaction.

Offline reconstruction through the real translation caller and `FrozenLiveTransport` structured decoder produced 24 rows at `.context/model-task-20260917/qwen235-translation-v3-recovery-rerun-diagnostic24-20260917-230735/caller-replay-output.json`; two rows have missing target fields because their final source requests exhausted 429. The source-only review covers all 72 fields at `source-only-review.json`: 17 native-copy passes, 3 coverage-missing fields, 39 reviewed passes, and 13 semantic errors. The errors include reversed 一折 discount meaning, literal French slang, invented cents currency, translated opaque model names, an asserted attack, misplaced tokenizer evidence, a stop/cannot-stop reversal, and technical-token mistranslation. Coverage and source-fidelity failures mean this route/configuration does not establish diagnostic parity or qualify for a wider cohort.


## Parent reconciliation

The final ledger is `parent-reconciled-review.json` beside the rerun. It treats the tokenizer-attachment case as uncertain, consistently with the incumbent, and adds the Chinese OpenCode Go plan-name omission/usage change. Final totals: 12 semantic-error fields, three missing fields, two uncertain fields, 38 reviewed-good generated fields and 17 native copies. Ten source posts have confirmed errors (eight semantic, two coverage), plus one additional uncertain source: **[10,11]/24 versus incumbent [7,8]/24**. This does not establish parity. The earlier source-only review remains preserved.
