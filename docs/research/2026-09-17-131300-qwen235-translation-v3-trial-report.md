# Qwen3-235B-A22B-Instruct-2507 translation v3 trial

`qwen235_translation_v3` retained the v2 model, GMICloud FP8 route, source-bound structured JSON-lines prompt, `temperature: 0`, price caps, and `max_concurrency: 1`. The only profile change was a frozen `min_request_interval_seconds: 4`. The live transport serialized request starts under a lock and records `pacing_wait_ms` for every request. It does not retry a failed request or use a fallback route.

The diagnostic24 contract is `.context/model-task-20260917/qwen235-translation-v3-diagnostic24-20260917-125905/contract.json`. All 55 frozen requests were consumed. Twenty-nine were received and 26 returned HTTP 429, leaving 15 of 24 sources without all required target locales. There were no retries, fallbacks, or database writes. Receipts totalled $0.003277715 for 14,518 input, 6,046 output, and 1,553 cache-read tokens; reported reasoning tokens were zero. Request latency p50 was 2.902 seconds, p95 18.450 seconds, maximum 115.470 seconds, and sum 359.527 seconds.

The pacing record confirms the new control executed: 33 of 55 requests waited, for 78.371 seconds total; the largest recorded wait was 3.223 seconds. Shorter waits occur when the preceding request's completion already consumed part of the four-second start interval. This is delivery evidence for the profile setting, not evidence of a precise provider quota. GMICloud still returned 429s despite serial, rate-spaced sends; the retained response records have no rate-limit headers or body from which to infer a fixed RPM quota.

The source-only review packet is `.context/model-task-20260917/qwen235-translation-v3-diagnostic24-20260917-125905/source-only-review-packet.json` (SHA-256 `179d5da686cb921d09fd8719fc1e89611b7da523c37056f14b7f650109d6c20b`). The complete 72-locale review is `source-grounded-review-v3.json` (SHA-256 `5c02199f2ec06a59ddd8140f711177b167e77a55d0c95a61922736b3dcd6fc07`): 26 coverage-error locales across 15 sources, 19 delivered locales deliberately unreviewed because their source already failed coverage, 24 passes among complete sources, and three semantic-error locales across two additional sources. The source interval is [17,17], against incumbent [7,8]; conservative R113 parity is not established.

The independently reviewed semantic failures are both source-general fidelity issues. Indonesian `pake opencode go sub` becomes an instruction to subscribe through OpenCode in Chinese and Japanese. The Korean technical term `토큰` becomes Chinese `代币` in the OpenRouter usage discussion, changing processed model tokens into coins. Even without those two semantic sources, the 15-source coverage failure establishes that this route/configuration cannot advance. This finding is bounded to the pinned Qwen checkpoint through the GMICloud FP8 route under the saved $0.0875/M input and $0.35/M output caps; it does not assess Qwen generally or authorize a higher-priced route.
