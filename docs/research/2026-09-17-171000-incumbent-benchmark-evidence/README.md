# Incumbent benchmark evidence

Seven verified artifacts retain exact matching translation/commentary source-output packets, source/caller equivalence receipt, subset usage, rubric, and the new direct 4.1 classifier contract plus all 48 raw-text/usage results. Reviewer drafts are excluded. Classification strict delivery is 24/24; semantic quality is not certified. Translation/commentary outputs were reused from the completed direct100 run after exact source/context/caller-hash checks, not regenerated.

Extract into a fresh directory and run `shasum -a 256 -c MANIFEST.sha256`. All archived member bytes have been verified.
