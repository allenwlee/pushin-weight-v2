# U18 selected 0731 runtime acceptance — September 16, 2026

Status: failed acceptance; no activation or deployment. Delivery target remains staging.

The selected cloud model is wired into the actual two-role classifier caller. Local integration tests passed, but this live request set did not satisfy the existing coverage or quality requirements. This result does not reopen model selection or the completed owner review. It identifies further work required before the selected model can be enabled.

## Frozen test

- Original 45 owner-reviewed posts; 20/20/5 batches; two independent roles; six transports, no retry, no repair, maximum concurrency three.
- OpenRouter `deepseek/deepseek-v4-flash-0731`, provider pin `deepinfra/fp8`, temperature 1, top-p 1, seed 42, reasoning disabled, plain JSON with strict local validation.
- Exact requests, source hashes, references, route/price receipt, and maximum reservation frozen before sending.
- Upper-bound reservation including funding fee: $0.01754650680; hard limit $0.05.
- Observed inference cost: $0.00268614; fee-adjusted: $0.00283387770; observed elapsed time: 10.183 seconds.
- All six transports returned and recorded cost; reasoning usage was zero. The conservative elapsed-time and cost checks passed. Three batches do not establish production capacity or p95 latency.
- Selected provider metadata attested DeepInfra; endpoint variant was request-pinned, not independently echoed in metadata.

## Result

Only 25/45 source posts produced valid complete merged classifications. The second 20-post content response placed `opinions_reactions` in the `outcome` field on eight decisions (D01, D02, D05, D06, D08, D12, D14, D15). That field accepts only `classified` or `context_missing`. The strict parser correctly rejected the entire role batch; all 20 pairs remain incomplete. No fields were guessed or repaired, and no incomplete classifications were published.

The valid rows also missed semantic requirements: 24/25 had null China nationalism, and the model often under-assigned post types. This is not solely a response-format failure.

Exact owner-amended-reference field matches, counting every unavailable row as a failure:

| Axis | Exact matches | Rate |
| --- | ---: | ---: |
| outcome | 23/45 | 51.1% |
| post_types | 5/45 | 11.1% |
| product_labels | 16/45 | 35.6% |
| sentiment | 14/45 | 31.1% |
| china_nationalism | 2/45 | 4.4% |
| us_nationalism | 2/45 | 4.4% |

This is agreement on a consumed development set, not unseen accuracy. The immutable original reference is scored separately in the machine report. Original product-label matches were 18/45, versus 16/45 against the owner-amended reference; the other displayed counts were unchanged. Existing aggregate empty-set metrics can count absent empty labels as matches, so the table uses explicit per-field matches and the separate mandatory 100% coverage gate. Neither scoring view passes the frozen floors.

## Local verification and follow-up

Before inference: 38 tests passed, including all three required PostgreSQL tests; none skipped. Tests cover selected request shape, no third semantic call, 20-post batching, duplicate/malformed rejection, once-only evaluation requests, billable error usage, and the real cycle/publication chain.

Post-run review found changed prompt semantics still used legacy revision identifiers. This was corrected without replaying the frozen test: legacy prompts were restored, the selected profile now has explicit role/merge revisions and request settings, and the publisher validates exact revision combinations. The follow-up PostgreSQL/runtime/pilot suite passed 62 tests with all three required database checks executed. A provider-free recapture proved all six outgoing provider requests remained byte-equivalent after the lineage fix. This changes lineage, not the recorded failed result. The frozen files and their source hashes remain the evidence for the run above.

The first prepared directory (`runtime-0731-acceptance-20260916-v1`) made no paid calls: a missing optional dotenv dependency stopped execution before its run marker or provider requests. The v2 runner uses literal secret parsing without that dependency. No secret values are present in either artifact set.

0731 translation and rich-commentary comparisons are separately required by U20/U21. Each role will compare the incumbent with 0731 on EN/ZH-CN/JA source-visible material and retain its own selection decision. Classifier results cannot establish translation or commentary quality.

## Evidence locations

- Frozen run: `.context/u18/runtime-0731-acceptance-20260916-v2/` (contract, exact requests, source packets, responses, measurements, candidate, report).
- Runner: `scripts/u18_runtime_0731_acceptance.py`.
- Safety tests: `tests/test_u18_runtime_0731_acceptance.py`.
- Runtime tests: `tests/test_u18_runtime_0731.py`, `tests/test_u18_two_role_foundation.py`.
- Frozen floor policy: `docs/analysis/2026-09-14-213123-u18-r98-control-fallback-pilot-contract.json`.

U18 stays incomplete; U18A and classifier-dependent staging activation remain gated. No production services, cron, database, or environment variables changed.

## Final local state

The lineage correction passed 62 runtime/pilot/PostgreSQL tests; 18 separate telemetry/config/acceptance tests passed. Offline replay of the six saved responses retained exactly 25 valid rows and identical classifications, now using `stage1-two-role-merge-0731-v1`. Replay receipt: `.context/u18/runtime-0731-lineage-replay-20260916.json`.

The U20/U21 prepare-only harness passed five tests and successfully saved the real 45-post comparison inputs under `.context/u20/translation-synthesis-0731-prepare-20260916-v1/`. Both model arms contain three translation and 45 synthesis requests, with 15 sources per language and length-stratified selection. There is no paid execution path yet. Current route prices, fee accounting, once-only transports, and the blinded semantic evaluation must be completed before selecting either role's model.
