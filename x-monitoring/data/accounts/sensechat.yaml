# {AGENT_ATTRIBUTION}
accounts:
  - handle: SenseTimeAI
    display_name: "SenseTime"
    role: official
    verified: false
    notes: Placeholder; operator must confirm SenseTime's official handle. 'Nova' alone is generic; brand-name co_occurrence gate in Call C helps.

# Manual staff list (you curate). v1.6 OR-collapse: these
# handles are folded into the per-brand account call alongside the
# official handle. Empty by default — add as you discover PM/dev
# accounts that should represent the brand.
staff: []
