{# {{AGENT_ATTRIBUTION}} #}
{# SVG-only htmx partial. Returned by /api/treemap.html and swapped into the
   <main id="treemap"> element on the front page. No <main> wrapper here —
   the wrapping element persists on the client so hx-trigger stays attached. #}
{{ treemap_svg | safe }}
