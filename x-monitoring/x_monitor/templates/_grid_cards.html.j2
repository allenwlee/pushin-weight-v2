{# {{AGENT_ATTRIBUTION}} #}
{# Renders just the card list — used by both the initial / page and the /api/grid.html poll route. #}
{% for card in cards %}
  {% include "_model_card.html.j2" %}
{% endfor %}
